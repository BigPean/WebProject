import React from 'react'

export const Home = () => {
    // 确保 Hooks 只在函数组件顶层调用
    // const [state, setState] = useState() // 如果有的话
    
    return (
        <div>
            Home
        </div>
    )
};